# BVR RL project — Python 3.11 recommended
# Install (one time) from the VS Code terminal or an Anaconda Prompt:
#     pip install -r requirements.txt
#
# Versions below match the machine the project was built on. If pip cannot find
# an exact version on your platform, removing the "==x.y.z" usually still works.
# NumPy MUST be 2.x: the saved models were created with NumPy 2 and won't load
# on NumPy 1.x. torch must be >=2.4 (older torch is incompatible with NumPy 2).
numpy>=2.0,<3
torch>=2.4
jsbsim==1.3.1
stable-baselines3>=2.3
gymnasium>=1.0
pymap3d>=3.0
cloudpickle>=3.0
tensorboard>=2.18
matplotlib
